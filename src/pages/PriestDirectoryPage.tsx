import { useEffect, useMemo, useState } from 'react';
import { CheckCircle2, Languages, MapPin, Search } from 'lucide-react';
import Layout from '@/components/layout/Layout'; import { ThemeProvider } from '@/hooks/use-theme'; import { Input } from '@/components/ui/input'; import { Badge } from '@/components/ui/badge';
type Priest={id:number;name:string;city:string;state:string;languages:string[];services:string[];verified:number};
const pujas=[{name:'Griha Pravesh',detail:'House-entry worship traditionally centered on Ganesha, Vastu and sacred fire.'},{name:'Satyanarayan Puja',detail:'A devotional Vishnu observance with katha, offerings and community prasada.'},{name:'Navagraha Homa',detail:'Fire offerings addressing the nine grahas within a lineage-specific ritual form.'},{name:'Shraddha Guidance',detail:'Family and regional ancestral observances requiring sensitive qualified guidance.'}];
export default function PriestDirectoryPage(){const[items,setItems]=useState<Priest[]>([]);const[q,setQ]=useState('');useEffect(()=>{void fetch('/api/priests').then(r=>r.json()).then(d=>setItems(d.items||[])).catch(()=>setItems([]));},[]);const shown=useMemo(()=>items.filter(x=>`${x.name} ${x.city} ${x.state} ${x.languages} ${x.services}`.toLowerCase().includes(q.toLowerCase())),[items,q]);return <ThemeProvider><Layout><main className="container mx-auto px-4 py-10"><p className="text-sm font-semibold uppercase tracking-[.2em] text-orange-700">Qualified guidance</p><h1 className="mt-2 text-4xl font-bold">Priest & Puja Directory</h1><p className="mt-3 max-w-2xl text-muted-foreground">Find regional language and ritual specializations. Listings are informational; contact details are added only after direct verification.</p><div className="relative mt-7 max-w-xl"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search city, language, or puja"/></div><section className="mt-8 grid gap-5 md:grid-cols-2">{shown.map(x=><article key={x.id} className="rounded-2xl border bg-card p-6"><div className="flex items-center gap-2 text-xs font-semibold text-emerald-700"><CheckCircle2 className="h-4 w-4"/>Directory verified</div><h2 className="mt-3 text-xl font-bold">{x.name}</h2><p className="mt-2 flex items-center text-sm text-muted-foreground"><MapPin className="mr-2 h-4 w-4"/>{x.city}, {x.state}</p><p className="mt-2 flex items-center text-sm text-muted-foreground"><Languages className="mr-2 h-4 w-4"/>{x.languages.join(', ')}</p><div className="mt-4 flex flex-wrap gap-2">{x.services.map(s=><Badge key={s} variant="secondary">{s}</Badge>)}</div><p className="mt-4 text-xs text-muted-foreground">Contact is pending direct consent and identity verification.</p></article>)}</section><section className="mt-14"><h2 className="text-2xl font-bold">Understand common pujas</h2><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{pujas.map(p=><article key={p.name} className="rounded-2xl bg-orange-50 p-5 dark:bg-orange-950/20"><h3 className="font-bold">{p.name}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{p.detail}</p></article>)}</div></section></main></Layout></ThemeProvider>}
